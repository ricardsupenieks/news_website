<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/styles/styleSearch.css">
    <title>Search</title>
</head>
<body>
    <div class="top">
        <h2 class="news">NEWS</h2>
        <form method="get" class="search_box">
            <input type="text" name="search" id="search" placeholder="Search" required>
        </form>
    </div>

    <div class="search_results">
        <h1>Search results for: "{{ searchTerm }}"</h1>
        <div class="flex-container">
        {% for article in articles %}
                <div class="article">
                    <a href="{{ article.url }}" target="_blank"><img class="image" src="{{ article.imageUrl }}" alt="{{ keyword }}"></a>
                    <div class="article_title"><b>{{ article.title }}</b></div>
                </div>
        {% endfor %}
        </div>
    </div>
</body>
</html>